machine:
  timezone:
    America/Los_Angeles
  ruby:
    version: 2.3.0 #デフォルトでは1.9.3でgemがインストールできない

dependencies:
  override:
   - bundle install
   - wget http://selenium-release.storage.googleapis.com/2.53/selenium-server-standalone-2.53.0.jar
   - java -jar selenium-server-standalone-2.53.0.jar:
       background: true

test:
  override:
   - rpsec sample_spec.rb
